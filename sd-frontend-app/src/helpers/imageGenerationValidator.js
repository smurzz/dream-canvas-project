export function promptValidator(prompt) {
    if (!prompt) return "Please describe your idea.";
    return '';
};

export function styleValidator(style) {
    if (!style) return "Please choose an artistic style.";
    return '';
};